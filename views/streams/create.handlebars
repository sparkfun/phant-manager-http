<div class="row">

  <div class="content">

    <h1>{{stream.title}}</h1>
    <p><strong>Fields:</strong> {{> renderFields stream}}</p>
    <p><strong>Tags:</strong> {{> renderTags stream}}</p>
    <p class="lead">{{{markdown stream.description}}}</p>
    <hr>

  </div>

</div>

<div class="row">

  <div class="col-md-6 col-md-offset-1 col-sm-6 col-sm-offset-1">

    <h4>Public URL</h4>
    <a href="/streams/{{publicKey}}" target="_blank"><pre>{{server}}/streams/{{publicKey}}</pre></a>

    <h4>Public Key</h4>
    <pre>{{publicKey}}</pre>

    <h4>Delete Key</h4>
    <pre>{{deleteKey}}</pre>
    <p>This key can only be used once.&nbsp; Keep this key secret, and in a safe place.&nbsp; <em>You will not be able to retrieve it.</em></p>

    <h4>Private Key</h4>
    <pre>{{privateKey}}</pre>
    <p>Keep this key secret, and in a safe place.&nbsp; <em>You will not be able to retrieve it.</em></p>

  </div>

</div>

<div class="row">

  <div class="content">

    <h4>Logging using query string params</h4>

    <p>Format:</p>
    <pre>{{server}}/input/[publicKey]?private_key=[privateKey]&{{sampleQueryFormat stream.fields}}</pre>

    <p>Example:</p>
    <a href="/input/{{publicKey}}?private_key={{privateKey}}&{{sampleQueryString stream.fields}}" target="_blank">
      <pre>{{server}}/input/{{publicKey}}?private_key={{privateKey}}&{{sampleQueryString stream.fields}}</pre>
    </a>

    <h4>Deleting a stream</h4>

    <pre>{{server}}/streams/{{publicKey}}/delete/{{deleteKey}}</pre>

  </div>

</div>
