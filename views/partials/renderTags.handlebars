{{#if tags.length}}

  {{#each tags}}
    <a href="/streams/tag/{{this}}">
      <span class="tag">
        {{this}}
      </span>
    </a>
  {{/each}}

{{else}}
  <em>(No tags definied)</em>
{{/if}}
